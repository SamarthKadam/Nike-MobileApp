const uiState = {
    isResultsPage:null
    };
    
    const uiReducer = (state = uiState, action) => {
      switch (action.type) {
        default:
          return state;
      }
    };
    
  
    export default uiReducer;